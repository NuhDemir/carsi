# Backend environment variables example
# Kopyalayın ve gerçek değerlerle `.env` dosyası olarak kaydedin (commit etmeyin).

# MongoDB bağlantı string'i (örnek: mongodb://user:pass@host:port/dbname)
MONGO_URI=

# Sunucu portu (opsiyonel, default 5000)
PORT=5000

# Çalışma modu (development veya production)
NODE_ENV=development

# JWT secret for signing tokens (örnek: complex string) -> .env içinde oluşturun
JWT_SECRET=your_jwt_secret_here

# Frontend origin for CORS (opsiyonel)
FRONTEND_URL=http://localhost:5173
